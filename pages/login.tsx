import Head from 'next/head';

export default function About() {
  return (
    <div>
      <Head>
        <title>Login</title>
        <meta name="description" content="About the app" />
      </Head>

      <main>
        <h1>Login</h1>
      </main>
    </div>
  );
}